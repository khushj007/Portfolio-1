import React from 'react'
import { propTypes } from 'react-bootstrap/esm/Image'

function Data(props) {
  return (
    <div className="item">
    <h5>{props.title}</h5>
    </div>
  )
}

export default Data